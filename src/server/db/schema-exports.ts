/**
 * This file exposes only the schema type definitions.
 * It's specifically designed to be safely imported from client components
 * without triggering server-side code execution on the client.
 * 
 * Import from this file when you only need schema types, not database connections.
 */

export * from "./schema"; 